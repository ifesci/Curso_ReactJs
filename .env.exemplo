# Para obter esses valores, basta acessar o painel do Supabase, entrar na página de seu projeto, clicar em "Project Settings" e, em seguida, em "Data API".
VITE_SUPABASE_URL="digite a URL do seu Supabase aqui"
VITE_SUPABASE_API_KEY="digite a chave de API do seu Supabase aqui"

# se quiser limpar o cache do git e readicionar os arquivos modificados, use o seguinte comando:
# git rm -r --cached . && git add . && git commit -m "Limpar cache do git e readicionar arquivos modificados"